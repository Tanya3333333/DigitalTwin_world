[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "real-world-mapping-visualizer"
version = "0.1.0"

[tool.setuptools]
packages = ["kernel", "kernel.interface"]
